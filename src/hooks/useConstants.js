import * as defaults from "../config/constantsList.json";
// import api constant endpoint usage
// declare the constant function

export default function useConstants() {
  let constants = defaults;
  // check stored constants // work with expiry
  // make the server call // to server to get all constants
  // store constants

  // return constants
  return constants;
}
